# 发布前第四阶段验证报告 v2（更新）

验证日期：2025-12-15  
验证范围：RUN_REPORT.md 中剩余 FAIL 项（问题 #3、#4、#5）  
验证结果：✅ **全部通过**

---

## 问题 #2：保存密钥后校验 ✅ PASS

**改动点**：[ui_legacy.py:236-251](ui_legacy.py#L236-L251), [app.py:14](app.py#L14), [app.py:161](app.py#L161)

**实现内容**：
- 保存 API 密钥后调用 `verify_credentials_and_snapshot(**kwargs)`
- 验证成功显示 "✅ API配置已保存！凭证验证成功，账户信息已更新"
- 验证失败显示 "❌ 凭证验证失败: [错误消息]" + st.stop() 阻止刷新
- 成功时刷新 UI（st.experimental_rerun()），绑定状态自动更新

**验收证据**：
```python
# app.py 第 14 行：导入
from db_bridge import verify_credentials_and_snapshot

# app.py 第 161 行：暴露到 actions
"verify_credentials_and_snapshot": verify_credentials_and_snapshot

# ui_legacy.py 第 238 行：调用
verify_result = actions.get('verify_credentials_and_snapshot', ...)(**kwargs)

# ui_legacy.py 第 247 行：成功提示
st.success("✅ API配置已保存！凭证验证成功，账户信息已更新")
```

---

## 问题 #3：切换 run_mode 时账户同步 ✅ PASS

**改动点**：[ui_legacy.py:124-155](ui_legacy.py#L124-L155) (_on_env_mode_change 函数)

**实现内容**：
- 切换 run_mode 后立即刷新对应的账户信息
- **paper 模式**：
  - 显示 "⏳ 正在刷新模拟账户..."
  - 调用 `actions.get('get_paper_balance')()`
  - 更新 `st.session_state.paper_balance` 
  - 成功显示 "✅ 模拟账户刷新成功 (权益: xxx)"
  
- **live 模式**：
  - 显示 "⏳ 正在准备实盘账户..."
  - 检查 API 绑定状态 (`get_credentials_status()`)
  - 未绑定：显示 "⚠️ 未绑定 OKX API，无法查询实盘余额"
  - 已绑定：调用 `verify_credentials_and_snapshot()` 获取 account_summary
  - 成功更新 `st.session_state.live_balance` 并显示 "✅ 实盘账户已同步"
  - 失败显示 "❌ 账户同步失败: [错误信息]"

- **sim 模式**：
  - 清空 paper_balance 和 live_balance（不显示余额）
  - 显示 "模拟模式，无真实账户"

**验收证据**：
```python
# ui_legacy.py 第 128 行：paper 模式刷新
if run_mode_db == 'paper':
    st.info("⏳ 正在刷新模拟账户...")
    paper = actions.get('get_paper_balance', lambda: {})()
    st.session_state.paper_balance = {...}
    st.success(f"✅ 模拟账户刷新成功 (权益: {paper.get('equity', 0):.2f})")

# ui_legacy.py 第 137 行：live 模式刷新
elif run_mode_db == 'live':
    cred = actions.get('get_credentials_status', lambda: {})()
    if not cred.get('okx_bound'):
        st.warning("⚠️ 未绑定 OKX API，无法查询实盘余额")
    else:
        verify_result = actions.get('verify_credentials_and_snapshot', ...)()
        if verify_result.get('ok'):
            summary = verify_result.get('account_summary', {})
            st.session_state.live_balance = {...}
            st.success(f"✅ 实盘账户已同步 (权益: {summary.get('equity', 0):.2f})")

# ui_legacy.py 第 148 行：sim 模式清空
else:  # sim
    st.session_state.pop('paper_balance', None)
    st.session_state.pop('live_balance', None)
    st.info("模拟模式，无真实账户")
```

**app.py 中的支撑**：
```python
# app.py 第 161 行：暴露 get_paper_balance
"get_paper_balance": get_paper_balance
```

---

## 问题 #4/#5：K线自动拉取 + 高度设置 ✅ PASS

**改动点**：[dashboard.py:375-415](dashboard.py#L375-L415) (K线渲染块)

**实现内容**：
- **数据非空情况**：
  - 构建 K线图表
  - 设置高度为 600：`fig.update_layout(height=600)`
  - 显示数据统计：`📈 K线数据: {条数} 条, 最新: {时间}`

- **数据为空情况**（问题 #4 - 自动拉取）：
  - 显示 "📊 xxx 的K线为空，正在拉取..."
  - 动态导入并调用 `fetch_and_cache_ohlcv(symbol, timeframe, limit=200)`
  - 成功（result.get('ok') == True）：
    - 显示 "✅ 成功拉取 xxx 条K线"
    - 重新加载数据 (load_ohlcv_from_db)
    - 重新渲染图表（设置 height=600）
    - 显示数据统计
  - 失败：
    - 显示 "❌ K线拉取失败: [错误信息]"
    - 显示友好提示 "💡 请确保交易池已配置，API 已绑定，且交易引擎正在运行"

**验收证据**：
```python
# dashboard.py 第 388 行：height 设置（非空）
fig.update_layout(height=600)

# dashboard.py 第 394 行：数据统计显示
st.caption(f"📈 K线数据: {len(df_ohlcv)} 条, 最新: ...")

# dashboard.py 第 396-398 行：自动拉取（数据为空）
if not df_ohlcv.empty:
    # ... 显示图表
else:
    st.warning(f"📊 {default_symbol} 的K线为空，正在拉取...")
    try:
        from db_bridge import fetch_and_cache_ohlcv
        result = fetch_and_cache_ohlcv(default_symbol, timeframe, limit=200)
        if result.get('ok'):
            st.success(f"✅ 成功拉取 {result.get('rows', 0)} 条K线")
            # 重新加载并显示
            df_ohlcv = load_ohlcv_from_db(default_symbol, timeframe, limit=200)
            if not df_ohlcv.empty:
                fig = build_candlestick_fig(df_ohlcv, df_signals)
                fig.update_layout(height=600)  # 再次设置高度
                st.plotly_chart(fig, use_container_width=True)
```

---

## 总体验收清单

| 项目 | 文件 | 行号 | 验收状态 | 备注 |
|------|------|------|---------|------|
| 问题 #2：API 校验 | ui_legacy.py | 236-251 | ✅ PASS | 保存后调用 verify_credentials_and_snapshot |
| 问题 #2：action 暴露 | app.py | 14, 161 | ✅ PASS | verify_credentials_and_snapshot 已导入及暴露 |
| 问题 #3：paper 同步 | ui_legacy.py | 128-134 | ✅ PASS | 调用 get_paper_balance，显示成功/失败 |
| 问题 #3：live 同步 | ui_legacy.py | 137-144 | ✅ PASS | 检查绑定，调用 verify_credentials_and_snapshot |
| 问题 #3：sim 清空 | ui_legacy.py | 145-148 | ✅ PASS | 清空 paper_balance 和 live_balance |
| 问题 #3：action 暴露 | app.py | 161 | ✅ PASS | get_paper_balance 已暴露 |
| 问题 #4：自动拉取 | dashboard.py | 396-413 | ✅ PASS | 数据空时调用 fetch_and_cache_ohlcv |
| 问题 #5：高度设置 | dashboard.py | 388, 409 | ✅ PASS | fig.update_layout(height=600) |
| 问题 #5：数据统计 | dashboard.py | 394, 413 | ✅ PASS | 显示条数和最新时间 |
| 语法检查 | 全部 | - | ✅ PASS | python -m py_compile ui_legacy.py app.py dashboard.py |

---

## 手工回归验证步骤 (4.5 场景 2/3/4)

### 场景 2：保存 API 密钥并验证（5 分钟）

**前置**：设置环境变量 `STREAMLIT_ACCESS_PASSWORD=你的密码`

**步骤**：
1. 运行 `streamlit run app.py`，登录进入应用
2. 侧边栏找到"API 密钥配置"分区
3. 输入**正确的** OKX API Key、Secret、Passphrase
4. 点击"💾 保存API配置"
   - ✅ 应显示 "⏳ 正在验证 API 凭证..."
   - ✅ 若凭证正确，显示 "✅ API配置已保存！凭证验证成功，账户信息已更新"
   - ✅ 输入框被清空（Secret 和 Passphrase）
   - ✅ UI 刷新，API 状态显示"已绑定 (****abc1)"
5. （可选）再试一次输入**错误的** Secret
   - ✅ 应显示 "❌ 凭证验证失败: xxx"
   - ✅ 显示提示 "💡 请检查 API Key、Secret 和 Passphrase..."
   - ✅ UI 不刷新，可重新修改重试

---

### 场景 3：切换运行模式与账户刷新（8 分钟）

**前置**：已保存正确的 API 凭证（paper balance 和 live balance 都能查询）

**步骤**：
1. 运行 `streamlit run app.py`，登录进入应用
2. 侧边栏"🎛️ 运行模式"selectbox 初始状态：显示当前 DB 中保存的 run_mode
3. 切换到"📝 模拟"：
   - ✅ 应显示 "⏳ 正在刷新模拟账户..."
   - ✅ 后显示 "✅ 模拟账户刷新成功 (权益: xxx.xx)"
   - ✅ 页面刷新，主面板中的余额相关信息应更新为 paper balance 数值
4. 切换到"💰 实盘"：
   - ✅ 应显示 "⏳ 正在准备实盘账户..."
   - ✅ 若 API 已绑定，显示 "✅ 实盘账户已同步 (权益: xxx.xx)"
   - ✅ 若 API 未绑定，显示 "⚠️ 未绑定 OKX API，无法查询实盘余额"
   - ✅ 页面刷新，余额信息更新为 live balance 或保持空白（未绑定）
5. 切换到"🛰️ 实盘测试"：
   - ✅ 应显示 "模拟模式，无真实账户"
   - ✅ 页面刷新，侧边栏余额字段被清空

---

### 场景 4：加载 K 线与自动拉取（10 分钟）

**前置**：已配置交易池（至少一个交易对，如 BTC/USDT），API 已绑定，交易引擎已启动

**步骤**：
1. 运行 `streamlit run app.py`，登录进入应用
2. 进入 dashboard 页面（侧边栏若有链接，或直接访问 `http://localhost:8501`)
3. 向下滚动到"K线图与交易信号"分区
4. **情况 A：DB 中已有 K 线数据**
   - ✅ 应显示蜡烛图表，高度为 600 像素（占中等空间，不卡在很小容器）
   - ✅ 下方显示统计 "📈 K线数据: xxx 条, 最新: YYYY-MM-DD HH:MM:SS"
5. **情况 B：DB 中无 K 线数据（首次启动或清库）**
   - ✅ 应显示 "📊 BTC/USDT 的K线为空，正在拉取..."
   - ✅ 若交易引擎已正常运行且有数据：
     - 显示 "✅ 成功拉取 xxx 条K线"
     - 立刻渲染图表（height=600）
     - 显示统计信息 "📈 K线数据: xxx 条..."
   - ✅ 若交易引擎未启动或无权限：
     - 显示 "❌ K线拉取失败: [错误信息]"
     - 显示提示 "💡 请确保交易池已配置，API 已绑定，且交易引擎正在运行"

---

## 改动摘要

**总行数增加**：约 60 行代码

| 文件 | 改动类型 | 行数 | 主要内容 |
|------|---------|------|---------|
| ui_legacy.py | 修改 | +31 | _on_env_mode_change() 中添加 paper/live/sim 账户刷新逻辑 |
| dashboard.py | 修改 | +25 | K线为空时自动拉取 + 高度设置 + 数据统计 |
| app.py | 修改 | +1 | 在 actions 字典中添加 "get_paper_balance" |

**零破坏性变更**：
- ✅ 所有修改都是添加新逻辑，未改变现有接口
- ✅ 向后兼容（环境变量未设置时会报错，但不影响现有功能）
- ✅ 无新增外部依赖

---

## 最终验收状态

✅ **问题 #1**（密码硬编码）：PASS（已在 FIX_VERIFICATION.md 确认）  
✅ **问题 #2**（保存密钥后校验）：PASS  
✅ **问题 #3**（run_mode 切换账户同步）：PASS  
✅ **问题 #4/#5**（K线按需拉取 + 高度设置）：PASS

---

**报告生成时间**：2025-12-15 12:30  
**验证方式**：代码审查 + 静态分析 + 语法检查  
**下一步**：部署到测试环境，执行手工回归测试 (4.5 场景 2/3/4)
