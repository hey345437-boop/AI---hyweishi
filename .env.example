# ============ OKX API 配置 (实盘环境) ============
# 从 OKX 官方获取的 API 密钥信息
# 重要：请勿在代码中硬编码这些值，必须通过环境变量或 .env 文件传入
OKX_API_KEY=your_live_api_key_here
OKX_API_SECRET=your_live_secret_here
OKX_API_PASSPHRASE=your_live_passphrase_here

# ============ 运行模式配置 ============
# 🔥 重要：本系统只支持两种模式
# - live: 实盘模式（真实下单）
# - paper_on_real: 实盘测试模式（实盘行情+本地模拟下单）
#
# 两种模式都必须使用实盘 API Key，禁止 demo/sandbox
# 默认值：paper_on_real
# 警告：live 模式会执行真实交易，请确保已充分测试
RUN_MODE=paper_on_real

# ============ 数据库配置 ============
# PostgreSQL 连接字符串（可选，若配置则使用外部数据库，否则使用本地 SQLite）
# 格式: postgresql://user:password@host:port/dbname
# 示例: postgresql://trading_user:secure_password@db.example.com:5432/trading_bot
DATABASE_URL=

# 数据持久化目录（可选，默认为项目根目录的 data/）
MYTRADINGBOT_DATA_DIR=./data

# ============ 交易策略配置 ============
# 交易对列表，格式: BTC/USDT,ETH/USDT,BNB/USDT（逗号分隔）
SYMBOLS=BTC/USDT:USDT,ETH/USDT:USDT,BNB/USDT:USDT

# 时间周期：1m, 5m, 15m, 1h, 4h, 1d 等
TIMEFRAME=1m

# 扫描间隔（秒）：建议不低于 2 秒以避免 API 限速
SCAN_INTERVAL_SEC=2

# ============ 交易所配置 ============
# 交易所市场类型：spot（现货） / swap（永续合约）
OKX_MARKET_TYPE=swap

# 交易模式：cross（全仓） / isolated（逐仓）
OKX_TD_MODE=cross

# 🔥 重要：OKX_SANDBOX 已废弃，强制为 false
# 本系统禁止使用 OKX 模拟盘（demo/sandbox）
# 即使设置为 true，也会被代码强制覆盖为 false
OKX_SANDBOX=false

# ============ 日志与调试 ============
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============ 安全配置 ============
# 🔐 加密主密钥（必需！用于加密 API 凭证）
# 警告：生产环境必须设置此值，不能使用默认值
# 建议：使用至少 32 字符的随机字符串
MYTRADINGBOT_MASTER_PASS=your_secure_master_password_here

# ============ Streamlit UI 配置 ============
# Streamlit 服务器端口
APP_PORT=8501

# 调试模式
APP_DEBUG=false

# Streamlit 访问密码（必需！用于 UI 登录）
# 警告：生产环境必须设置此值
# 开发环境可留空，将使用默认密码（不安全）
STREAMLIT_ACCESS_PASSWORD=your_ui_password_here

# ============ 风控配置 ============
# 单笔订单最大金额（USDT），超过此金额的订单将被拒绝
# 默认值：1000
MAX_ORDER_SIZE=1000

# 单日最大亏损限制（占账户权益的百分比）
# 超过此限制将自动暂停交易
# 默认值：0.10（10%）
DAILY_LOSS_LIMIT_PCT=0.10

# ============ 系统容错配置 ============
# 循环中允许的最大错误次数（超过则停止）
MAX_CYCLE_ERRORS=10

# 遇到致命错误是否立即退出（true / false）
EXIT_ON_FATAL=false

# ============ 代理配置 (可选) ============
# 若网络环境需要代理，可配置以下代理服务器
http_proxy=
https_proxy=
socks5_proxy=
